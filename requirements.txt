# Litigation & Court Data Analytics — pinned dependencies
# Versions below are the ones the published results were generated with.
# Python 3.10+

# --- data handling ---
pandas==2.3.3
numpy==2.2.6

# --- statistical modelling ---
scipy==1.15.3            # chi-square, Kruskal-Wallis
statsmodels==0.14.6      # Wilson intervals, two-proportion z-test
lifelines==0.30.0        # Kaplan-Meier, Cox PH, AFT models

# --- visualisation ---
plotly==6.9.0

# --- optional: static PNG export of the charts (not needed to run the pipeline) ---
# kaleido==0.2.1
